FRONTEND_PORT=8088
REDIS_LOCAL_PORT=16379
ALLOWED_ORIGINS=http://localhost:8088,http://127.0.0.1:8088
# Docker: default docker-compose.yml uses Redis;
# use -f docker-compose.memory.yml for memory mode (no Redis service).
MEMORY_MAX_ROOMS=1000
